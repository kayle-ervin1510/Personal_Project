name: Execute Cypress Test

on:
    pull_request:
        branches:
            -main

    push:

jobs:
    test_tests:
        
        runs-on: ubuntu-latest

        steps:
            - name: Setup Node 
              run: actions/setup-node@v4